# Python dependencies. Install into a virtualenv:
#   python -m venv .venv
#   .venv/bin/pip install -r requirements.txt        (macOS/Linux)
#   .venv\Scripts\pip install -r requirements.txt    (Windows)
#
# External tools are NOT pip-installable — see README (ffmpeg, docker, osmium).

flask>=3.1
pywebview>=6.2
numpy>=2.0
pycairo>=1.28
shapely>=2.0
timezonefinder>=8.0
